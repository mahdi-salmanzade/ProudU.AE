import type { Metadata } from "next";
import { FlagGuidelinesContent } from "./FlagGuidelinesContent";

export const metadata: Metadata = {
  title: "UAE Flag Guidelines · ProudU.AE",
};

export default function FlagGuidelinesPage() {
  return <FlagGuidelinesContent />;
}
